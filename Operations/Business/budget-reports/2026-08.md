# Budget report — 2026-08

**Run:** 2026-08-03 (the 08-01 slot fired late; see *Run notes*) · **Report #1** — no prior month on file, so there is no baseline to compare against.
**Sources:** `BIZ_01_Elementum_Expense_Report.md` (as-of 2026-07-07) · `PM_03_Day_Log.md` (07-30 entry + doc audit) · `BIZ_02_Elementum_Validation_Sprint.md` · live worker config (`workers/llm/wrangler.jsonc`) · git.
**Not in scope:** statement reconciliation. No financial credentials, portals or APIs were touched — repo docs and live config only.

---

## 1 · Expected spend this month

| Line | Plan | Expected Aug | Verified how |
|---|---|---|---|
| Anthropic Claude Max (development) | Max 20× | **$200.00** | BIZ_01 §1 — the №1 line, larger than everything else combined |
| Cloudflare Registrar — elementum.life | annual | **≈ $2.50** amortized *(~$25–30/yr, **still unconfirmed**)* | BIZ_01 §1; OWNER-3 open |
| Anthropic API — §4.3 consultant | pay-per-use, Phase 0 | **≈ $0–2** (bounded ≤ ~$7) | see §2 |
| Cloudflare Workers ×4 | Free | $0 | free-tier headroom intact; cron free |
| Supabase | Free | $0 | idle-pause mitigated by the push cron's hourly traffic |
| Stripe | per-transaction | $0 fixed | no sales this month to date |
| Google Cloud / Search Console / GitHub | free | $0 | structurally $0 (OAuth unmetered, geocoding on Open-Meteo) |
| **All-in monthly** | | **≈ $202.50 – $205** | |
| **Product runtime only** | | **≈ $2.50 – $5** | |

**No unexpected or unmatched lines.** Every service in BIZ_01 §1 still maps to the plan recorded there, and no new billable service appeared in the tree. One drift item, in our favour: BIZ_01 §1 already reads "Workers ×4", so the old ×3 miscount is closed.

## 2 · The Anthropic API line — what is and isn't knowable from here

Guardrails verified **live** in `workers/llm/wrangler.jsonc`, not assumed from the doc:

- `MONTHLY_BUDGET_USD: "50"` — the kill-switch is real and wired (`index.js:137` reads the month's `llm_usage` rows before every call).
- `DAILY_CAP: "30"` — per-user message cap, live.
- `OWNER_IDS` non-empty → **still Phase 0, owner-only.** One human can spend on this line.

Actual dollars live in the Supabase `llm_usage` table, which this report does not read. The bound is what matters and it is tight: one user × 30 msgs/day at Sonnet-cached rates is ~$7/mo worst case, and realistic owner testing is well under $2. **This line cannot surprise us while Phase 0 holds** — the moment `OWNER_IDS` is emptied (INFRA-1), it becomes the only variable line in the budget and deserves a real reading each month.

## 3 · Deferred triggers

| Item | Cost | Status | Recommendation |
|---|---|---|---|
| **Cloudflare Workers Paid** | $5/mo | ⚠️ **Calendar date arrived — condition not met** | **Hold, and re-anchor the trigger** |
| Supabase Pro | ~$25/mo | Not fired | Hold |
| Apple Developer (Lantern Digital) | $99/yr | Not fired | Hold — do not enroll early |

**Workers Paid — the one that needs your attention.** BIZ_01's backlog writes the trigger as *"~4 wks before first external users (early Aug for Sept ship, or validation-sprint wave)."* It is early Aug. But the condition behind the date is not satisfied: no ship date is scheduled, and the validation sprint that would have supplied the "wave" closed its window **2026-07-12 with all ten boxes unchecked** — Build A (PostHog + Cloudflare Analytics) and Build B (ESP) are still unbuilt, and the 07-30 audit confirms **zero** `analytics_engine` bindings anywhere in the tree.

So the $5 would currently buy a capability nothing consumes. My recommendation is to **not spend it yet**, and to change the trigger from a calendar month to the thing it actually depends on: *"4 wks before the first external-user date, once that date exists."* That is a two-line owner edit to the backlog table in BIZ_01 — I have not made it.

Worth being explicit about the coupling, because it runs the other way too: Workers Paid gates WAE, WAE gates the analytics decision, and analytics is the validation sprint's Build A. **If you want the sprint to run in September, the $5 has to land ~4 weeks before it — meaning this decision is really a by-product of setting the ship date, not of the calendar.** Set the date and the trigger answers itself.

Supabase Pro and Apple sit behind the same absent gate (beta marketing / Phase B scheduled). Nothing about them changed this month. The Apple line stays deliberately unpaid: the $99 membership year starts at purchase, and the D-U-N-S (14-193-9711) is already secured, so enrollment stays ready at zero cost.

## 4 · Burn and runway

Revenue side, from BIZ_01 §2 and §5: net per sale is **$8.44** (Founding $9) and **$6.49** (Self-Report $6.99). The only purchase on record is the owner's own **$9 self-test** — so **external revenue this month is $0**, against ~$202.50 of all-in cost. All-in break-even is **~24 passes/mo**; runtime-only break-even is **~1 pass per quarter**.

One observation on the $200 line, since it *is* the budget. The repo has had **no commits since 2026-07-30 17:42** — three idle days at the time of writing, roughly $20 of plan against no build output. That is not a problem in itself (idle days happen, and the preceding week ran 18–19 commits/day), but it is the mechanism behind BIZ_01 §6's open to-do: *"periodically revisit whether the Max plan tier still matches the build phase."* The honest framing is that this is a **development investment with a fixed monthly clock**, and the clock does not pause for groundwork weeks. Worth a look if the doc/data-station phase runs long.

**Runway observation:** runtime cost is ~$2.50/mo, so the shipped product can stay alive essentially indefinitely on free tiers plus one domain — there is no infrastructure cliff ahead. The entire financial question is whether the $200/mo development line is a bounded investment phase or a standing cost, and that is a decision, not a forecast.

## 5 · Money guardrails still open (both $0 to close)

- **HK-3 — Anthropic console spend cap is still unset.** BIZ_01 §4 #3 calls this the "outer wall" (~$25 recommended). The worker-side $50 kill-switch is live and verified, but it is *our* code guarding *our* usage; a console limit is the hard stop that survives a bug or an abuse case the worker doesn't see. Open ~25 days. **This is the highest-value zero-cost action on the list.**
- **HK-4 / OWNER-3 — domain auto-renew unverified, and the real registration price still unconfirmed.** Both close in one visit to Cloudflare → Billing, and the price is the last figure in this report that is an estimate rather than a fact. Auto-renew is the higher stake of the two: it is domain-loss risk, not accounting.

## 6 · Recommended doc edits (for you or a session — not made here)

1. **BIZ_01 §1, Anthropic API row (`:21`)** — still reads "not created / pending this decision" 🔵, while `:93` records it decided and live since 2026-07-07. Should be 🟢 live, Phase 0, owner-only. Carried doc-audit finding; a real self-contradiction in the cost inventory.
2. **BIZ_01 deferred backlog, Workers Paid row (`:106`)** — re-anchor the trigger to the ship date per §3 above.
3. **BIZ_01 §3, Phase-1 LLM line** — the "$5–15" figure assumes Phase 1 is open; while Phase 0 holds, actual is ~$0–2. Fine as a projection, but note the phase.

## 7 · Run notes

- **This run is 2 days late.** The 08-01 slot fell on a Saturday, which clashes with the weekdays-only automation law; it caught up on Monday app launch — the same machine-local pattern recorded as `BUG-20260729-1` for the morning pipeline. Worth deciding whether the monthly budget slot should be exempted from the weekday law (it is monthly, not daily) or moved to the first weekday of the month.
- **The write path is fixed.** The 07-30 doc audit flagged that `.claude/settings.json` allowlisted this report's write at the dead `Documents/Business/budget-reports/**` prefix. It now reads `Operations/Business/budget-reports/**` (`:60-61`), and this file saved cleanly. **Finding closed.**
- Repo state at run time: clean tree, `main` level with `origin/main` — the merge/deploy block described in the 07-30 entry is resolved.
