# Budget report — 2026-09

**Run:** slot fired on schedule 2026-09-01 (Tuesday — last month's weekend clash did not recur) · **filed 2026-09-03**, the session's clock advanced mid-run · **Report #2** — first month with a baseline to compare against (`2026-08.md`).
**Sources:** `BIZ_01_Elementum_Expense_Report.md` (as-of 2026-07-07, content unchanged since) · `PM_03_Day_Log.md` (**stale — newest entry 2026-07-30**, see *Run notes*) · `PM_02` task board · live worker configs (all four `wrangler.jsonc`) · `package.json` · git (119 August commits) · live scheduled-task registry.
**Not in scope:** statement reconciliation. No financial credentials, portals or APIs were touched — repo docs and live config only.

---

## 1 · Expected spend this month

| Line | Plan | Expected Sep | Verified how |
|---|---|---|---|
| Anthropic Claude Max (development) | Max 20× | **$200.00** | BIZ_01 §1 — still the №1 line, larger than everything else combined |
| Cloudflare Registrar — elementum.life | annual | **≈ $2.50** amortized *(~$25–30/yr, **still unconfirmed, 58 days**)* | BIZ_01 §1; OWNER-3 open |
| Anthropic API — §4.3 consultant | pay-per-use, Phase 0 | **≈ $0–2** (bounded ≤ ~$7) | see §2 |
| Cloudflare Workers ×4 | Free | $0 | all four configs re-read: **zero paid-plan bindings** anywhere |
| Supabase | Free | $0 | idle-pause mitigated by the push cron's hourly traffic |
| Stripe | per-transaction | $0 fixed | no sales on record |
| Google Cloud / Search Console / GitHub | free | $0 | structurally $0 (OAuth unmetered, geocoding on Open-Meteo) |
| **All-in monthly** | | **≈ $202.50 – $205** | |
| **Product runtime only** | | **≈ $2.50 – $5** | |

**No unexpected or unmatched lines, and this month I can say so from evidence rather than inventory.** Two checks worth recording:

- **`package.json` is byte-identical across all of August** — `git diff` from 07-31 to HEAD returns nothing. 119 commits landed and not one added a dependency, let alone a billable service.
- **No wrangler config declares a paid-plan feature.** Grepping all four for `analytics_engine`, `durable_objects`, `queues`, `r2_buckets`, `d1_databases`, `kv_namespaces`, `usage_model` and `limits` returns **zero hits**. The stack is free-tier by construction, not by luck — and this is the same evidence that keeps the Workers Paid trigger unfired (§3).

The four workers in BIZ_01 §1 all resolve: `elementum` (root config), `elementum-llm`, `elementum-push`, `elementum-stripe-webhook`.

## 2 · The Anthropic API line — and the change coming to it

Guardrails re-verified **live** in `workers/llm/wrangler.jsonc` and `workers/llm/index.js`, not assumed from the doc:

- `MONTHLY_BUDGET_USD: "50"` — kill-switch wired; `index.js:150,161` reads the month's `llm_usage` rows and refuses before every call.
- `DAILY_CAP: "30"` — per-user cap, live at `index.js:155`.
- `OWNER_IDS` non-empty → **still Phase 0, owner-only.** One human can spend on this line, worst case ~$7/mo.

Actual dollars live in the Supabase `llm_usage` table, which this report does not read. The bound is what matters and it is tight.

**What changed is the context around it, not the number.** The record says INFRA-1 (the Phase 1 flip — literally `OWNER_IDS=""`) was parked pending "a dedicated voice-polish session." **That session happened, and it took most of August.** The month's work was almost entirely voice and tone: `REA_16 The Voice` born as the tone canon, the `humanized-prose` benchmark distilled and applied, the charter taken through rounds 6 and 7 with the worker redeployed twice (`79b484f`, `c278b30`, `b374d75`).

So the stated blocker on INFRA-1 has substantially been worked off. I am not recommending the flip — that is a product call, and the `/legal` privacy amendment is its own gate — but the budget consequence is worth stating now rather than in the month it fires: **the moment `OWNER_IDS` is emptied, this becomes the only variable line in the budget**, guarded by $50 of our own code and nothing else. Which moves §5 from housekeeping to sequencing.

## 3 · Deferred triggers

| Item | Cost | Status | Recommendation |
|---|---|---|---|
| **Cloudflare Workers Paid** | $5/mo | Not fired — condition still unmet, one month on | **Hold; the re-anchor edit is still open** |
| Supabase Pro | ~$25/mo | Not fired | Hold |
| Apple Developer (Lantern Digital) | $99/yr | Not fired | Hold — do not enroll early |

**Workers Paid.** Last month this row was the one asking for attention: its calendar date ("early Aug") had arrived while the condition behind it had not. A month later the condition is *still* not met — **zero** `analytics_engine` bindings anywhere in the tree (re-verified today across all four configs), no ship date scheduled, the validation sprint still unrun. The $5 would still buy a capability nothing consumes. **Hold.**

The recommended fix has not been made: BIZ_01's backlog row still reads *"~4 wks before first external users (early Aug for Sept ship…)"* — a date now a month in the past, describing a September ship that was never scheduled. It is a two-line owner edit: re-anchor to *"4 wks before the first external-user date, once that date exists."* The coupling I flagged last month is unchanged and now has a month of evidence behind it: Workers Paid gates WAE, WAE gates analytics, analytics is the validation sprint's Build A. **Set the ship date and this trigger answers itself; leave it unset and the row reads stale again in October.**

Supabase Pro and Apple sit behind the same absent gate. Nothing about either changed. The Apple line stays deliberately unpaid — the $99 membership year starts at purchase, the D-U-N-S (14-193-9711) is secured, and PM_02 STORE-1 keeps enrollment ready at zero cost.

## 4 · Burn and runway

Revenue side, from BIZ_01 §2/§5: net per sale is **$8.44** (Founding $9) and **$6.49** (Self-Report $6.99). The only purchase on record remains the owner's own **$9 self-test** — **external revenue is $0**, against ~$202.50/mo all-in. All-in break-even is **~24 passes/mo**; runtime-only break-even is **~1 pass per quarter**.

**On the $200 line, August reverses last month's observation, and I want to correct the impression I left.** The August report noted three idle days and framed the Max plan as a fixed clock running against no output. August answered that: **119 commits across 15 active days** — roughly **$13.30 per active build day** — and September opened at 20 commits in its first three days. The plan tier matched the build phase this month; BIZ_01 §6's standing "revisit whether Max still matches the phase" to-do is not urgent right now.

What August bought is worth naming precisely, because it is not shipped surface: the reading corpus and its voice. The month locked ELEMENT_PAIR v2/v3 (×25, ×50), the mechanism corpus v2 (×25), the POSITION anatomy corpus (×70, 522 units), the TG_PATTERN axis, and the tone canon governing all of it. **That is content inventory, not features** — it does not move the QA sweep and it does not appear in the bundle, so a reader watching only the app surface would again mistake a heavy month for a quiet one.

**Runway observation:** unchanged, and still the honest headline — runtime is ~$2.50/mo, so the shipped product can stay alive indefinitely on free tiers plus one domain. There is no infrastructure cliff ahead in any of the three deferred triggers. **The entire financial question is still whether the $200/mo development line is a bounded investment phase or a standing cost** — a decision, not a forecast, and one that only a ship date answers.

## 5 · Money guardrails still open (both $0 to close)

- **HK-3 — the Anthropic console spend cap is still unset, now ~58 days open.** This was last month's "highest-value zero-cost action" and it has become more than that. The worker-side $50 kill-switch is live and verified (§2), but it is *our* code guarding *our* usage; the console limit is the hard stop that survives a bug, an abuse case, or a misconfigured deploy. **With the Phase 1 flip's stated blocker now largely worked off (§2), the correct sequence is: set the console cap first, flip `OWNER_IDS` second.** In that order it costs nothing and takes one visit.
- **HK-4 / OWNER-3 — domain auto-renew unverified, real registration price still unconfirmed.** Both close in one visit to Cloudflare → Billing. Auto-renew is the higher stake: domain-loss risk, not accounting. The price is still the only estimate rather than a fact in §1 of this report — as it was in the last one.

## 6 · Recommended doc edits (for you or a session — not made here)

All three are **carried unchanged from the 2026-08 report**; BIZ_01 has had no content edit since the July restructure rename.

1. **BIZ_01 §1, Anthropic API row (`:21`)** — still reads "not created / pending this decision" 🔵 while `:93` records it decided and live since 2026-07-07. Should be 🟢 live, Phase 0, owner-only. A real self-contradiction inside the cost inventory.
2. **BIZ_01 deferred backlog, Workers Paid row (`:106`)** — re-anchor the trigger to the ship date per §3. Now actively misleading: it names a month that has passed.
3. **BIZ_01 §3, Phase-1 LLM line** — the "$5–15" figure assumes Phase 1 is open; while Phase 0 holds, actual is ~$0–2. Fine as a projection, but note the phase.

## 7 · Run notes

- **The slot fired on time.** Live registry: `elementum-budget-report`, `lastRunAt 2026-09-01T15:50Z`, cron `17 10 1 * *`, enabled. 2026-09-01 was a Tuesday, so the weekday-law clash that made the August run two days late did not recur. **Forward look: 2026-10-01 is a Thursday and 2026-12-01 a Tuesday — both clean — but 2026-11-01 is a Sunday, so the clash returns in two months.** The August report's open question (exempt the monthly slot from the weekday law, or move it to the first weekday) is still worth answering before then; it costs nothing to decide now.
- **The write path held.** `.claude/settings.json:61-62` allowlists `Operations/Business/budget-reports/**` and this file saved cleanly. Second clean run — the July finding stays closed.
- ⚠️ **`PM_03_Day_Log.md` has no entry since 2026-07-30 — a full month of the log this report is meant to read for trigger status.** The EOD project-manager routine is enabled with a recent `lastRunAt`, so it is firing without producing entries; the day log's own preamble calls itself "THE one file for daily project state." **I substituted git history, the live worker configs, the task registry and PM_02 for it**, which is why §1–§4 lean on verified evidence rather than the log. A reader should know the usual trigger-status source was unavailable — and this is an ops finding for the Agent Manager, not a budget one.
- Repo state at run time: clean tree, `main` level with `origin/main` (0/0), no QA sentinels present.
