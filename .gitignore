# Claude worktrees and session artifacts (any depth) — but root
# .claude/agents/ holds versioned automation infrastructure.
**/.claude/*
!.claude/agents/
Elementum/

# Editor/backup cruft
*.bak
*.tmp
*.orig

# Large reference bundle (downloaded export — not for version control)
Reference/
