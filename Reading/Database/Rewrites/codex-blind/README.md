# Rewrites by codex-blind

Filed 2026-09-23 from 2026-09-23 geng golden blind by `ComparativeAnalysis/Prompts/file-rewrites.mjs`. `by_axis/json/<AXIS>/<cell>.codex-blind.json` mirrors the final template station's shape (sparse: only the fields this model wrote; `candidates` = the rewrite, `$original` = the shipping text, `$gate_findings` per field). `by_variable/json/<var>.codex-blind.json` is the pivot per variable. `by_axis/md/` is the readable twin. Nothing here is truth: adopt a field with `node adopt.mjs codex-blind <AXIS>/<cell> <field path>` on the owner's ruling.
