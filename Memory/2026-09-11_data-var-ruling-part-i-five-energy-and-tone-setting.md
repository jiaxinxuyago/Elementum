# Handoff — 2026-09-11 · Session "Data Var Ruling Part I (five energy) and Tone Setting"

Written on the primary laptop for cross-machine continuation (cloud sessions / second laptop).
Head commit at writing: `4d5b5ff` — design(reading): the journey restructure (three-beat catalogue + stem-literacy line).

## Standing laws (verbatim-critical, owner-ruled)

- **Station-first pipeline:** `Reading/Database/templates/by_axis/json/` = THE editing surface →
  `tools/build-template-twins.mjs` regen (twins + by_variable pivot) → REA docs → deliberate
  `src/content` transcription → `tools/export-reading-templates.mjs` (default = field-level OWED audit;
  `--harvest` is DESTRUCTIVE, never run casually) → `tools/voice-audit.mjs` → `tools/qa-journey-sweep.mjs`
  → commit `-F msgfile` → push (auto-deploys elementum.life).
- **NEVER edit `Elementum_App/tools/backup-customer-data.mjs`** — findings report-only, forever.
- `archive/` + BIZ_03 are history — never rewritten.
- **Content decisions are owner-ruled one by one** (never batch content rulings; questionnaires only when
  the owner asks for a fix worklist). Surface ruling-vs-locked-corpus collisions as QUESTIONS, never
  invent reconciling doctrine.
- **Register a REA_16 §2c row BEFORE authoring any content variable** (voice-audit is registry-driven;
  register/status cells must be SINGLE words; budget is read from the cell, not the notes).
- **Function nouns law:** Body · Mind · Expression · Action · Order = the elemental-relation vocabulary
  everywhere; seat/shadow nouns are Codex ore.
- **THE VOCABULARY ZONE (REA_16):** B1–B2 conversational English, instant-ESL test, Gen-Z
  self-description register; accuracy first · basic daily vocabulary · no invented words · keyword
  uniqueness enforced only within same-element sibling pairs.
- **DO NOT change the dominance wheel design** (hard constraint; legend rails + fnlab labels are the
  only additions, 2026-09-10).
- Italic is BANNED app-wide (DES_04 §AM.10 v3 — faux-oblique defect).
- Design HTMLs must track the live app — debt currently DEFERRED until the owner calls the layout
  settled (it grows with every design pass).
- Restart the Vite dev server after script-written content files or on stale-HMR errors before trusting
  fresh-context tests.
- Live demo = elementum.life; app changes must reach it (Stop hook auto-deploys; manual deploy if the
  hook can't run).
- **The automation stack (9AM QA / fallback detector / EOD routines) runs ONLY on the primary laptop.**
  Never set it up on a second machine — duplicate emails and racing auto-merge/deploy paths.

## Current workstream state

- **THE READING JOURNEY RESTRUCTURE SHIPPED (2026-09-10, `4d5b5ff`):** catalogue = three beats
  (hero + jbridge → YOUR ENERGY MANUAL → YOUR ENERGY MAP with SEEK/EASE rails); SKIP→EASE law with
  core-on-ease for Overfueled charts (YOURSELF tag), mirrored for Underfueled; `stem_keywords` retired
  everywhere → Codex ore, replaced by the derived stem-literacy line (`庚 · YANG METAL
  [· CAST FROM YOUR DAY OF BIRTH]`); P4 = identity only ("The Sign", causal gifts/shadows headers,
  dm-handoff back to the manual). Docs cascaded: REA_02 §5c amendment + §7, REA_16 §6 ×2 + registry
  row, REA_03 rows, DES_04 §AM.11 ×3.
- **Function-reading corpus COMPLETE + quality-hardened both axes:** ELEMENT_PAIR ×25
  (define/advise, dips → `__ore` TG_PATTERN backlog) + ELEMENT_GOD ×50 ledger-only fn_reading
  (3 doors/word, trait→scene→outside rotation, weighted assembly ≥60% 2+1 / <60% 2+2); adj_chips
  zone-swept ×55; prose zone pass ×33; accuracy pass ×77 doors.
- **Gates at head:** journey sweep 16/16 · eslint 0 errors / 57 warnings (standing baseline) ·
  voice-audit 44 registry rows clean (~644 PEND inventory across 9 unruled surfaces = the future
  remediation map).

## Pending / parked (owner-rulable)

- 金_木 pair polish (scrutiny item 15) — parked.
- Scrutiny items 16–18 (angle collisions / rhythm passes / pair minors) — dropped as
  diversity-motivated under the keyword core principles; only reopen if the owner re-frames them
  as accuracy.
- ~~Five unmerged `claude/*` fix branches~~ — **ALL FIVE MERGED INTO MAIN 2026-09-11 (owner: "check
  if there's merge conflict and resolve"; head `766baa6`)**: share-card token literals `29ae8a1` ·
  font alignment `1ef1874` · DevBar barrel import `fc9ff96` (superseded by later main, resolved ours) ·
  italic sweep `7b0ac04` (resolved: SC font from main + italic deletions from the branch) ·
  journey.css purge `432b39a` (resolved: purged 645-line base + every later main-side change
  re-applied — wheel 320, SC fonts, italic-free, all restructure blocks; one rescue: the copy1-only
  live rule `.jny[data-journey="scroll"] .jbridge{display:flex}` carried into the survivor; liveness
  sweep of all 518 deleted selectors vs journey JSX = zero live losses). journey.css now 699 lines,
  single copy. Gates after merges: sweep 16/16 · eslint 0/57 baseline. Remote branches can be deleted
  once the owner confirms.
- Design-HTML sync debt (catalogue + element page + P4) — deferred until layout is called settled.
- Parked decision batch: REA_03 §7 #3/#4 + journey definition-of-locked checklist (offer when the
  owner has decision bandwidth).

## Pointers

- Doc roots: `Reading/Documents/` (REA_01–06, REA_16 = tone canon + §2c audit registry) ·
  `Design/Documents/` (DES_04 §AMENDMENT = locked UI law) · `Operations/` (registry =
  `Operations/README.md`; runbook PM_01, board PM_02).
- Station: `Reading/Database/templates/` (by_axis = truth, by_variable = generated pivot).
- Full session history: primary laptop, session `local_566ac0a0…` ("Data Var Ruling Part I"),
  transcript + auto-memory under `C:\Users\NOBOD\.claude\projects\D--Elementum-Elementum-Project\`.
